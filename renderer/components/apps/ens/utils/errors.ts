export const ENS_ERRORS = {
  ALREADY_REGISTERED: "Name is already registered",
  INVALID_NAME: "Invalid ENS name",
  REGISTRATION_FAILED: "Failed to register name",
  // ... more error types
} as const;
